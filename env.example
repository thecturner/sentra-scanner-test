# Copy this to .env and edit safely for local runs.
# Docker Compose will auto-read .env in the same directory.

AWS_REGION=us-east-1
AWS_PROFILE=default

# Only set the below if you are NOT using a profile or role
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# App config . tweak as your code expects
RESULTS_BUCKET=sentra-results-bucket
RESULTS_PREFIX=scans/
RESCAN_MANIFEST_KEY=manifests/s3-scan.json
THREADS=8
LOG_LEVEL=INFO

# Which script to run
SCANNER_SCRIPT=scanner.py
SCANNER_ARGS=
